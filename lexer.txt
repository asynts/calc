function expand_expression() {
    if( match_infix_operator() ) {
        must match_expression();
        true;
    }

    if( match_postfix_operator() ) {
        true;
    }

    false;
}

function match_expression() {
    if( match_unary_operator() ) {
        must match_expression();
    }
    elif( match_integer() ) {
    }
    elif( match_identifier() ) {
        match_function_arguments();
    }
    else {
        false;
    }

    while( expand_expression() ) {
    }

    true;
}
